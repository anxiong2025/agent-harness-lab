[project]
name = "agent-harness-lab"
version = "0.0.0"
description = "Build an agent harness from first principles, one stage at a time."
requires-python = ">=3.12"
dependencies = [
    "openai>=3.3.0",
    "python-dotenv>=1.2.3",
]
